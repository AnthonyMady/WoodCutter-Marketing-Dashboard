; Personal project — bypass Booking's internal artifactory mirror.
; pnpm reads this BEFORE ~/.npmrc, so the global registry setting is overridden
; just for this directory tree.
registry=https://registry.npmjs.org/
